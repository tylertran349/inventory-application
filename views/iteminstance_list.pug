extends layout

block content
  h1= title

  ul
    each val in iteminstance_list
      li
        a(href=val.url) #{val.item.name}, #{val.condition} - $#{val.price}

    else
      li There are no items available right now.
